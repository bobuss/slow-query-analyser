import React from 'react'
import ReactDOM from 'react-dom/client'
import SlowQueryDashboard from './SlowQueryDashboard.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SlowQueryDashboard />
  </React.StrictMode>,
)